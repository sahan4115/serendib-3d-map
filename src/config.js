// Shared world constants. Terrain lies in the XZ plane, +Y up.
export const CFG = {
  terrainWidth: 100, // world units across (E–W)
  heightScale: 10, // peak elevation in world units (vertical exaggeration)
  seaLevel: 0,
}
